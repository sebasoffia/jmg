{
  "name": "jeminguey",
  "version": "1.0.0",
  "description": "Validador de contenido en español",
  "scripts": {
    "start": "npx serve -s . -l $PORT",
    "dev": "npx serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
